[package]
name = "hyperact"
version = "0.0.0"
description = "A keyboard-first action launcher"
authors = ["farzher"]
edition = "2021"
build = "build.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-global-shortcut = "2"
